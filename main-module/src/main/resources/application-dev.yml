server:
  port: 8081
  shutdown: graceful
#  字典翻译
#dictman:
#  primaryKey: id    #默认表主键是解析id
#  resultList: data.records #默认data.list
spring:
  main:
    # bean懒加载
    lazy-initialization: false
  lifecycle:
    timeout-per-shutdown-phase: 30s #关机缓冲时间,默认30s
  servlet:
    multipart: #文件传输配置
      max-file-size: 100MB #单个数据大小限制
      max-request-size: 100MB #请求总数据大小限制
      enabled: true  #是否启用分段上传支持
  data:
    #===================== Redis配置 =====================
    redis:
      database: 1 #缓存库编号
      host: 127.0.0.1
      port: 6379
      password:
      timeout: 50000 #超时时间(单位：秒)
      lettuce: #Lettuce为Redis的Java驱动包
        pool:
          max-active: 8 # 连接池最大连接数
          max-wait: -1ms  # 连接池最大阻塞等待时间（使用负值表示没有限制）
          min-idle: 0 # 连接池中的最小空闲连接
          max-idle: 8 # 连接池中的最大空闲连接
  jackson: #序列化和反序列化json框架
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    #    此处须配置自己的数据库地址和端口，否则启动失败
    url: jdbc:mysql://127.0.0.1:3306/study?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC
      &allowPublicKeyRetrieval=true
    username: root
    password: root
    hikari:
      max-lifetime: 60000
#  system-module模板使用配置
#  kafka:
#    bootstrap-servers: localhost:9092
#    consumer.group-id: myGroup


############## Sa-Token 配置 (文档: https://sa-token.cc) ##############
sa-token:
  # token名称 (同时也是cookie名称)
  token-name: token
  # token有效期，单位s 默认30天, -1代表永不过期
  timeout: 360000
  # token临时有效期 (指定时间内无操作就视为token过期) 单位: 秒
  activity-timeout: -1
  # 是否允许同一账号并发登录 (为true时允许一起登录, 为false时新登录挤掉旧登录)
  is-concurrent: true
  # 在多人登录同一账号时，是否共用一个token (为true时所有登录共用一个token, 为false时每次登录新建一个token)
  is-share: true
  # token风格
  # random-32 随机32位字符串
  token-style: random-32
  # 是否输出操作日志
  is-log: false
  #关闭控制台banner
  is-print: off

#业务相关
api:
  yangli: 3204cdf5a2a0c36778c880a461b6ea5d
  xiaohua: 3607764bbcfbb9d0651884970d376468

#日志配置
#logging:
#  config: classpath:logback-larry.xml
#  file:
#    path: d:/toolserver/logs/${spring.application.name}
#  level:
#    #    应用自身的日志等级
#    admin: info
#    org.springframework.web: error


#监控地址 127.0.0.1:8088/monitor
#management:
#  endpoints:
#    web:
#      base-path: /monitor
#      exposure:
#        include: '*'
#        # 排除端点
#  #        exclude: shutdown
#  server:
#    port: 8088
#  endpoint:
#    health:
#      show-details: always
#    shutdown:
#      enabled: true
